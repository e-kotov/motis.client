% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_geocode.R
\name{mc_geocode}
\alias{mc_geocode}
\title{Autocompletion & geocoding that resolves user input addresses including coordinates}
\usage{
mc_geocode(
  text = NULL,
  language = NULL,
  type = NULL,
  place = NULL,
  placeBias = NULL,
  .return_as = NULL,
  .json_parser = NULL,
  .headers = NULL,
  .auth = NULL,
  .throttle_rate = NULL,
  .build_only = NULL,
  .server = NULL,
  .endpoint = NULL,
  .referer = NULL,
  .req_options = NULL,
  .handle_response = NULL,
  .json_auto_unbox = NULL
)
}
\arguments{
\item{text}{the (potentially partially typed) address to resolve}

\item{language}{language tags as used in OpenStreetMap
(usually ISO 639-1, or ISO 639-2 if there's no ISO 639-1)}

\item{type}{Optional. Default is all types.

Only return results of the given types.
For example, this can be used to allow only \code{ADDRESS} and \code{STOP} results.

Allowed values: ADDRESS, PLACE, STOP.}

\item{place}{Optional. Used for biasing results towards the coordinate.

Format: latitude,longitude in degrees}

\item{placeBias}{Optional. Used for biasing results towards the coordinate. Higher number = higher bias.}

\item{.return_as}{How to return the response. One of 'list' (default), 'string', or 'raw'.}

\item{.json_parser}{JSON parser when \code{.return_as = 'list'}. One of 'RcppSimdJson' (default) or 'jsonlite'.}

\item{.headers}{Named list of additional HTTP headers to include.}

\item{.auth}{Bearer token string or a function \code{function(req) req} to apply custom auth.}

\item{.throttle_rate}{Requests per second (numeric). If set, throttling is applied.}

\item{.build_only}{If TRUE, return the unperformed request object.}

\item{.server}{Override the server URL for this call.}

\item{.endpoint}{A string to override the path for this specific request (e.g., '/v2/users/{id}'). If provided, it will be used instead of the default path from the specification.}

\item{.referer}{Optional Referer header value.}

\item{.req_options}{Named list of curl options passed to \code{httr2::req_options()}.}

\item{.handle_response}{Optional function \code{function(resp) ...} to post-process the response.}

\item{.json_auto_unbox}{If TRUE (default), auto-unbox scalar JSON values for request bodies.}
}
\description{
Autocompletion & geocoding that resolves user input addresses including coordinates
}
